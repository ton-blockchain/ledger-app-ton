#pragma once

/**
 * Show main menu (ready screen, settings, about, quit).
 */
void ui_menu_main(void);

/**
 * Show about submenu (version, developer).
 */
void ui_menu_about(void);

/**
 * Show settings submenu.
 */
void ui_menu_settings(void);
